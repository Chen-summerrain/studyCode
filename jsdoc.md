```
/**
 *
 * @description 介绍
 * @constructor 标明这是一个构造函数
 * @alias 别名
 * @example
 * 示例
 * @extends {Type} 继承某一函数
 * @param {String} 一个函数的参数，参数类型,值域
 * method = [get|post] 可选值域包括get和post，get是直接请求，post是提交数据
 * @property {Type[,Type,...]} propertyName 对象的属性描述
 * @return {Type[,Type,...]} 返回对象类型
 * @type {Type[,Type,...]} 变量类型
 * 
 * 
 */
```
